package com.types

enum class ViewString {
    MENUBAR, SIDEBAR, HOME
}

enum class Theme {
    LIGHT, DARK
}

enum class SideContent {
    NONE, SIGNIN_FORM, SIGNUP_FORM, VIEW_CLOUD_FILES, HOTKEYS, ACCOUNT_SETTINGS, CLOUD_FILE_DETAIL
}

enum class ToastMessage {
    SUCCESS, ERROR
}